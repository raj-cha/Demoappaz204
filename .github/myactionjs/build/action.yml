name: Dotnet build 
description: "Demo"

runs:
  using: composite
  steps:
      - name: Checkout
        uses: actions/checkout@v4
 
      - name: setup SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '7.x'

  
